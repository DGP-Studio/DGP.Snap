<UserControl x:Class="DGP.Snap.Window.LiveWallpaper.LiveWallPaperView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DGP.Snap.Window.LiveWallpaper"
             mc:Ignorable="d" 
             d:DesignHeight="540" d:DesignWidth="960"
             Width="1920" Height="1080"
             Loaded="UserControl_Loaded"
             >
    <Grid>
        <MediaElement x:Name="myMediaElement" Volume="0" Stretch="UniformToFill" >
            <MediaElement.Triggers>
                <EventTrigger RoutedEvent="MediaElement.Loaded">
                    <EventTrigger.Actions>
                        <BeginStoryboard>
                            <Storyboard>
                                <MediaTimeline Source="{Binding VideoSource}" Storyboard.TargetName="myMediaElement" RepeatBehavior="Forever" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger.Actions>
                </EventTrigger>
            </MediaElement.Triggers>
        </MediaElement>
    </Grid>
</UserControl>
